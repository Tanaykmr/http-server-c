{
	"devDependencies": {
		"husky": "^8.0.0",
		"lint-staged": "^15.2.7"
	},
  "scripts": {
    "lint:fix": "find . -name '*.c' -or -name '*.h' | xargs clang-format -i"

  },
	"husky": {
		"hooks": {
			"pre-commit": "clang-format -i *.c"
		}
	}
}
